<!DOCTYPE html>
<html>
<head>
  <title>SharedApp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= yield %>

<p>This app is connected to:
  <ul>
    <% ActiveRecord::Base.connection.instance_variable_get(:@config).except(:password).each do |key, value| %>
      <li><strong><%= key %>:</strong> <%= value %></li>
    <% end %>
  </ul>
</p>
</body>
</html>
